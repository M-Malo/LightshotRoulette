To launch the lightshot roulette, you may need to install the keyboard library.
>pip install keyboard

Then you can run the program in an IDE or in a CMD with the command >python "lightshot roulette.py"